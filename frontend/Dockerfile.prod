# Build-only container for production frontend assets
FROM node:20-alpine AS build

WORKDIR /frontend

# Install deps
COPY package*.json ./
RUN npm ci

# Copy source and build
COPY . .
RUN npm run build

# No runtime stage: docker-compose mounts ./frontend/dist into nginx
